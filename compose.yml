services:
  web:
    image: oven/bun:1
    container_name: flores-amarillas-dev
    working_dir: /app
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/node_modules
    command: sh -c "bun install && bun run dev --host 0.0.0.0"
    restart: unless-stopped
