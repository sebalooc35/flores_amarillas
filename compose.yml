services:
  web:
    image: oven/bun:1
    container_name: san-valentin-dev
    working_dir: /app
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: sh -c "bun install && bun run dev --host 0.0.0.0"
    restart: unless-stopped
